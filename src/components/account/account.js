import React from "react";

const Account = () => {
  return (
    <div
      style={{
        // display: "flex",
        // flexDirection: "column",
        // gap: "3rem",
        paddingTop: "3rem",
        marginTop: "128px",
        // alignItems: "center",
      }}
    >
      Account
    </div>
  );
};

export default Account;
